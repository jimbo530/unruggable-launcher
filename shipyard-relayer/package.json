{
  "name": "shipyard-payout-keeper",
  "version": "1.0.0",
  "description": "Pushes claimable USDC fee-share from each Shipyard launch's FeeShareDistributor out to the 100 crew NFT holders (claimAll), with an optional reactor.execute() top-up. DRY-RUN by default, import-safe, non-custodial.",
  "main": "shipyard-payout-keeper.js",
  "private": true,
  "scripts": {
    "start": "node shipyard-payout-keeper.js",
    "dry": "node shipyard-payout-keeper.js",
    "once": "node shipyard-payout-keeper.js --once",
    "check": "node --check shipyard-payout-keeper.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0"
  },
  "engines": {
    "node": ">=18"
  }
}
