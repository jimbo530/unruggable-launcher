# FLAWS — what the bot players hit

_The bots are the game's first players AND its QA. Every entry here is a real block or rough edge
they met inside the rules. This file is the build backlog's front door._

- **2026-07-01** [harness] fight-loot payout rail gated OFF (SEAS_LOOT_FIRE): server-verified wins bank zero copper/goods — the designed rung-0 income doesn't land.
- **2026-07-01** [harness] crab/fish catch dispenser not wired: the free zero-resource bootstrap income is read-only intel.
- **2026-07-01** [harness] day-1 achievement needs house attest before a bot can self-claim — new pawns' richest early move waits on a manual step.
- **2026-07-01** [harness] job wages at ~zero flow (guard token has no volume → no Aave yield): clocked-in pawns accrue time but no coin.
- **2026-07-01** [citizen] Attest backlog freezes achievement income: 50 Mayor 1d rungs (crewId 0x8C1f...0545:0-49) have been EARNED-but-unattested for days — the Citizen cannot claim until the Coordinator runs achievement-claim-fire.cjs/guard-ladder-keeper.cjs. The game's stated richest income rail is blocked by an off-game manual step; the house attest needs to run on a schedule.
- **2026-07-02** [brawler] fight loot not landing: 8+ consecutive server-verified bilge-rats WINs since 2026-06-30 with copper/gold/hold all still zero — the fight payout keeper appears off, so the Brawler's only income rung banks nothing despite winning.
- **2026-07-02** [worker] work read outcome not journaled: work{} on 2026-07-01 returned ok but the journal recorded only the word 'work' — no job catalog, wages, or pawn ids — forcing a repeat read the next tick
- **2026-07-02** [fisher] Voyage never lands: sail to (1,0) returns ok but pawn location stays null across 2+ days of ticks, blocking the entire location-gated fish loop (catch/sell) at the server-clock/arrival registration step.
- **2026-07-02** [trader] Achievement scan misses Black Tide pawns: claim-achievement scanned 70 but found citizenOwnedGuardPawns=0 for wallet 0xA9c8...3747, so ~8 days of server-verified spar wins register neither as claimable nor blocked-on-attest — the whole earn-by-achievement rail may be dead-ended for non-Guard pawns.
- **2026-07-02** [brawler] pawn id not discoverable: work clock-in requires --pawn <distributor:tokenId> but neither the pawns read nor the live-state command picture exposes my pawn's distributor or tokenId — only a byShip count — so a one-pawn player can't construct the required argument without digging.
- **2026-07-02** [brawler] guard clock-in unreachable for solo player: work requires --pawn <distributor:tokenId> but pawns/live-state/claim reads only ever show a byShip count for The Black Tide — two ticks spent hunting an id the UI never prints; expose distributor:tokenId in the pawns read.
- **2026-07-02** [worker] work read omits pawn ids: two consecutive work{} reads (2026-07-02) returned only a generic catalog summary — no distributor:tokenId for my Redrum Raiders at port_royal and no per-job wage rates — so the clock-in args the tool itself requires cannot be assembled from its own read output.
- **2026-07-02** [fisher] work clock-in fails silently: `work` with jobId+pawn exits 1 printing only 'work' — no validation message naming the expected jobId or distributor:tokenId format, leaving a one-pawn player unable to discover their own pawn id from the catalog read or the error path.
- **2026-07-03** [citizen] Opaque tool failure: `fish` (sell attempt, 2026-07-02) exited 1 echoing only its own name — no error text, so the player can't tell bad args from a broken rail; the tool should print WHY it failed.
- **2026-07-03** [worker] pawn id unobtainable by any read: work clock-in requires distributor:tokenId but work{}, pawns, and the live-state command picture all return only ship-name counts — no read rail exposes the id the act rail demands, hard-blocking the wage path for a 1-pawn player
- **2026-07-04** [worker] zero-capital player has no income rail: fish catch is a GOLD→FISH swap (requires gold funding + presence at loc 8004) and work clock-in requires a distributor:tokenId that no read tool exposes — so a brand-new 1-pawn worker with 0 coins at port_royal cannot earn a first copper through honest labor at all; the peasant climb's rung 0 is unreachable without outside funding or a fight.
- **2026-07-04** [fisher] fight WIN pays no loot: bilge-rats fight on 2026-07-03 returned WIN (server-verified) but zero copper/goods reached my wallet or hold a full day later — the loot payout keeper appears to be off, making the only unblocked rung-0 earner unpaid.
- **2026-07-04** [trader] Work catalog omits pawn ids: the no-jobId work read is documented to return 'my pawns' current jobs + accrued time' but twice returned only the generic job list for wallet 0xA9c8...3747, so a player cannot discover the distributor:tokenId required to clock in — the wage rail is unreachable for anyone who doesn't already know their pawn id.
- **2026-07-05** [worker] work read still omits pawn ids (re-check 2026-07-05): the no-args work catalog read remains the only sanctioned path to the distributor:tokenId that clock-in requires, and it still does not return it — the wage rail stays hard-blocked for a 1-pawn player at port_royal
- **2026-07-05** [worker] work read still omits pawn ids (re-check 2026-07-05 tick 2): the no-args work catalog remains the only sanctioned source of the distributor:tokenId clock-in demands and still does not return it — wage rail stays hard-blocked for a 1-pawn zero-capital player at port_royal
- **2026-07-05** [fisher] pawn id undiscoverable: work clock-in requires --pawn <distributor:tokenId> but no read anywhere (pawns, work catalog, wallet) prints my Sol del Mar's distributor:tokenId — the wage rail is unusable because its required key is never exposed to the player.
- **2026-07-05** [citizen] convert-winnings silent no-op: returned ok with no usd arg but wallet USDC stayed 0, which made the dependent water-pawn exit 1 — if it also no-ops with explicit usd:1 this tick, the GOLD→USDC leg of the runSequence is broken.
- **2026-07-05** [citizen] GOLD→USDC rail missing: water-pawn requires USDC but convert-winnings converts cbBTC→GOLD only (per its own menu description) — it returned ok twice (no-arg and explicit usd:1) while wallet USDC stayed 0, so rung 3 (levels) is unreachable for a GOLD-rich, cbBTC-poor player until a GOLD→USDC hop is wired.
- **2026-07-05** [worker] crab dispenser unwired blocks last zero-capital rail: at port_royal with 0 coins, work clock-in needs a distributor:tokenId no read exposes and fish catch is a gold-funded swap at loc 8004 — crabbing is the only harvest-type (capital-free) income mechanic, and its dispenser is not wired, so a 1-pawn zero-capital worker still cannot earn a first copper by any honest means.
- **2026-07-06** [citizen] GOLD sink dry: 'build plan mill' prices a structure but cannot execute — StructureFactory is not deployed, so a GOLD-rich player (9.3k GOLD) has no live path to convert claim income into production; both compounding rails (water-pawn via USDC, build via factory) are blocked at once.
- **2026-07-06** [trader] Work catalog still omits own-pawn ids: the documented 'my pawns + accrued time' section never appears in the bare work read — the id had to be recovered from the pawns tool instead; keeping this filed since the wage rail is undiscoverable from the work tool alone.
- **2026-07-06** [citizen] fish tool opaque failure: 'fish' exited 1 at Port Royal with no error body — just the word 'fish' — giving the player nothing to correct (args? location? wiring?); the sell rail can't be proven until it fails loudly.
- **2026-07-06** [worker] work read hides pawn job status: work {} at Port Royal returns only the job catalog + clock-in usage line — it never reports whether a given pawn is clocked in, on which job, or accrued time, so a player cannot verify a clock-in without spending gas on a duplicate attempt.
- **2026-07-06** [fisher] fight payout lag: server-verified WIN at 13:27 paid 0 copper (wallet 96 before and after, ~25 min later) — payout keeper appears off or batching, so the advertised 'loot pays' rung silently accrues instead of landing
- **2026-07-06** [trader] quote route errored: SALT→COPPER quote exited 1 with no error detail at Port Royal — a read-only pricing call should return a price or an honest reason, not a bare failure; hold value is unpriceable until this works
- **2026-07-06** [citizen] fish sell exit-1 persists with explicit args: 'fish' with action:sell, usd:0.25 at Port Royal still exits 1 printing only the word 'fish' — the wrapper itself appears broken (read-mode loop works, act-mode dies before any error body), blocking the only USDC self-fund path for watering.
- **2026-07-06** [worker] quote rail exits 1 with no error text: quote {tokenIn:'SALT', tokenOut:'COPPER', amount:10} failed exit 1 on two consecutive ticks at Port Royal with no diagnostic output, so a player cannot price held goods or tell a broken route from a bad symbol — and the menu exposes no goods-sell tool to act on a quote anyway.
- **2026-07-06** [citizen] quote exits 1 with no error body: read-only 'quote' with tokenIn:COD tokenOut:USDC amount printed only the word 'quote' and died — same opaque failure as fish sell, so the whole COD→USDC pricing path fails silently, blocking the only USDC self-fund rail and giving the player nothing to correct.
- **2026-07-06** [trader] quote tool broken tool-wide: both SALT→COPPER and RATIONS→COPPER exit 1 with no error detail at Port Royal — pricing any hold good is impossible, so the entire GOODS TRADER rung is unpriceable, not just one route
- **2026-07-08** [citizen] claim window advances without payout landing: between 07-06 and 07-08 the claimable start moved tokenId 10→15 and the per-claim reward fell 409→370 GOLD (pool drained ~10%), yet my wallet gold is unchanged at 13215 and my claim attempts on the listed ids returned 'nothing claimable for the given --pawns' — either the READ lists rungs the claim path won't accept, or claims are being consumed without crediting the Citizen wallet.
- **2026-07-08** [trader] build plan returns no plan or price: 'build plan mill' and 'build plan farm' at Port Royal return only the DRY/no-StructureFactory disclaimer with zero cost/output numbers — a PLAN mode should still price the design even when deployment is gated, otherwise the BUILDER rung is unpriceable on top of the broken quote tool
- **2026-07-08** [citizen] quote exits 1 on COD→GOLD too: after COD→USDC and fish sell died bare, the bisect quote tokenIn:COD tokenOut:GOLD also printed only 'quote' and exited 1 — either the quote tool is broken for all goods routes or COD itself fails symbol/route resolution; no error body is emitted to tell the player which.
- **2026-07-08** [worker] quote rejects all goods symbols silently: SALT→COPPER, RATIONS→COPPER, and SALT→GOLD each exit 1 with no error text at Port Royal, while the GOLD→COPPER control quote succeeds — so goods symbols are unsupported in the quote rail, and with no goods-sell tool in the menu either, a player's entire starting hold (141 units of food goods) is unpriceable and illiquid.
- **2026-07-08** [trader] fight wins pay nothing: 3+ consecutive server-verified bilge-rat WINs at Port Royal left the purse unchanged at 95 copper — the would-claim payout never lands (payout keeper off or claim rail unwired), so the PEASANT fight rung earns zero despite winning
- **2026-07-08** [citizen] claimable READ serves cached data while claims require live ids: the claim-achievement READ came back cached:true showing a window (tokenId 15+) that my last two successful batches may have already spent, yet the claim path rejects stale ids with 'nothing claimable' — the tool demands fresh ids but doesn't guarantee a fresh READ to source them from.
- **2026-07-08** [citizen] quote exits 1 bare on SALT→GOLD too: after COD→USDC, fish sell, and COD→GOLD all died with no error body, SALT→GOLD failed identically — quote breakage covers all goods inputs, blocking every price-discovery and self-fund (fish-sell → USDC → water-pawn) path; no diagnostic output tells the player whether it's the tool or the routes.
- **2026-07-08** [citizen] quote goods-route resolution broken (bisect complete): GOLD→COPPER quotes fine but COD→USDC, COD→GOLD, SALT→GOLD, and fish sell all exit 1 with no error body — the goods symbol/route layer is the fault, and it silently blocks every self-fund path (fish-sell → USDC → water-pawn) while emitting zero diagnostics.
- **2026-07-09** [fisher] quote tool errors: quote COD→GOLD (amount 11) has exited 1 on three separate ticks with explicit args, blocking my ability to price held goods before a voyage.
- **2026-07-09** [trader] fish sell errors bare: fish action:sell usd:0.10 at Port Royal exits 1 with no detail across multiple ticks — if this fails again with explicit args, the only live COD-sell rail is unusable and the PEASANT-to-trader rung has zero working income at Port Royal
- **2026-07-10** [citizen] goods quote rail dead: quote exits 1 bare for ANY goods input (COD→USDC, COD→GOLD, SALT→GOLD) while coin→coin (GOLD→COPPER) works, and fish action:sell fails the same way at Port Royal — goods route/symbol resolution is broken below the wrapper, blocking the fish→USDC→water-pawn self-fund path entirely.
- **2026-07-10** [brawler] fight loot payout stalled: three server-verified bilge-rats wins (2026-07-05, 07-08, 07-09) paid 0 copper — wallet frozen at 98 since the 07-06 batch landed; payout keeper appears off again
- **2026-07-10** [worker] work clock-in gives no receipt: live work {jobId:haul, pawn:0x4ECe491951B759363bCBAF75389a202Fe0584080:0, mode:1} at Port Royal ran on 4 consecutive ticks (2026-07-09) and returned only the bare string 'work' — no tx hash, no accrued-time/wage figure, no error — while copper stayed at 97, so a solo worker cannot tell if the clock-in landed, and the honest-labor rung is unverifiable.
- **2026-07-10** [worker] work read omits pawn state: work {} at Port Royal returns only the job catalog — it never lists my pawn's current job or accrued time, so 4 live haul clock-ins (2026-07-09) remain unverifiable and the honest-labor rung is dead-ended for a solo worker until the read or the clock-in receipt is fixed.
- **2026-07-10** [brawler] guard clock-in not landing: two consecutive work guard attempts (2026-07-10) returned ok-echoes but onGuardJob still reads 0 — either the clock-in silently fails or the status flag never updates
- **2026-07-10** [trader] work read omits pawn job status: the no-arg work read at Port Royal returns only the static job catalog — it never shows my pawns' current jobs or accrued time as documented, so a barter clock-in for 0x2E2AB7ae48876f1b4497A04d864C025f7DF58e1f:53 cannot be verified and the wage rail is unauditable
- **2026-07-10** [brawler] guard clock-in still unverifiable: work-catalog READ describes jobs but has never shown my pawn's own job status or accrued time, and the claimable scan's onGuardJob flag serves cached values — there is no reliable fresh read to confirm a clock-in landed
- **2026-07-10** [worker] crab dispense ignores live mode: fish {action:'crab'} at loc 2006 (on-site, live trading ENABLED) returned 'DRY — no tx, re-run with --execute (CITIZEN_ALLOW_LIVE=1)', but the action menu exposes no execute arg on fish — the bot has no way to trigger the live HarvestGround.dispense, dead-ending the only zero-capital income rail at the moment of payoff.
- **2026-07-11** [brawler] claim-achievement scan ignores non-Citizen guard pawns: fresh (cached:false) scan reads onGuardJob=0 and citizenOwnedGuardPawns=0 while the work read confirms my pawn 52 employed on guard ~24h — brawler-owned guard pawns seem invisible to the achievement scan, blocking any guard-rung attest for my line
- **2026-07-11** [fisher] fish sell fails silent: at open water (1,0), fish action=sell exits 1 with no refusal message, so it neither sells nor names the required port hex — the location-gate error text is the only in-game way to discover Port Royal's coordinates and it's blank.
- **2026-07-11** [citizen] GOLD has no live sink: ~32k GOLD sits idle — build lists mill/farm but StructureFactory is not deployed, and water-pawn needs USDC with no GOLD→USDC converter exposed (convert-winnings is cbBTC→GOLD only). The endgame gold of the richest rail has nowhere to go.
- **2026-07-12** [citizen] Guard accumulated time frozen at zero: all 50 guard pawns show currentRun ~16d22h but accumulated 0d0h0m — either the accrual read is broken or 16 days of watch-duty wage time is not banking anywhere.
- **2026-07-12** [brawler] loot payout keeper not crediting verified wins: 4+ server-verified bilge-rats WINs since 2026-07-09 with zero wallet delta — copper pinned at 98 the whole time. Per the design truth this is the real harness flaw (the keeper, not the fight rail): wins report ok but bank nothing.
- **2026-07-12** [worker] wage payout semantics unexposed: pawn 0x4ECe...4080:0 employed at haul with 10d 22h accrued at Port Royal, copper frozen at 97 — no auto-drip; work's mode 1|2 arg is undocumented, work {jobId} without pawn errors, and work {} shows only the catalog, so there is no safe in-game way to convert accrued wage time to copper without blind-firing mode:2 live and risking the accrual.
- **2026-07-12** [citizen] Job read cannot resolve pawns 50-69's employer: myPawnJobs shows 'unknown target 0x2E2AB7ae48876f1b4497A04d864C025f7DF58e1f' for 20 pawns — the job catalog is missing that contract's mapping, so a fifth of my crew's employment (and any wage it accrues) is unreadable.
- **2026-07-12** [worker] quote tool returns no data: SALT→COPPER quote at Port Royal (2026-07-12) returned only the bare string 'quote' — no price, no amountOut, no error — making it impossible to price held goods before selling; the read-only pricing rail is effectively blind.
- **2026-07-12** [trader] water-pawn READ omits level/flow data: the no-execute READ on pawn 53 at Port Royal returns ok with only the live-gating note ('READ-ONLY — no transaction sent...') and no level, flow, or cost numbers, despite the menu promising 'No execute = READ level + cost' — this blocks diagnosing whether the barter wage (employed 6d, accumulated 0d 0h 0m, copper flat at 95 for ~72h) is flow-gated or a dead accrual rail.
- **2026-07-12** [citizen] Fight wins bank nothing: server-verified WINs at 07-12T13:24 and 13:47 paid zero copper/goods (copper flat at 199, hold unchanged) — the loot payout keeper appears OFF, so the from-zero income rail is dead while it stays down.
- **2026-07-13** [citizen] Silent clock-in failures: work returned ok for pawns 74 and 75 on 07-12T14:09 but both read employed:false in the 07-13 myPawnJobs — the work tool reports success on clock-ins that never land, so batch employment can't be trusted without a next-tick read.
- **2026-07-13** [worker] goods sell rail missing: at Port Royal the only live sell (fish sell) rejects held seed COD — 'not enough FISH: have 0, need 10.00' — it only accepts freshly-caught FISH tokens; with quote returning bare 'quote' (no price data) and trade being buy-only, the entire seed food hold (141 units across SALT/RATIONS/APPLE/HONEY/COD/ALE/JERKY/CINNAMON) has no path to coin anywhere in the action menu.
- **2026-07-13** [fisher] loot payout keeper off 48h+: three consecutive server-verified bilge-rats WINs (2026-07-11 13:27, 2026-07-12 13:02, 2026-07-12 13:26) have paid 0 copper against a flat 96-copper baseline — the from-zero fight rail verifies wins but banks nothing, stalling every zero-capital climber at rung 0.
- **2026-07-13** [worker] build plan returns no price: build plan mill and build plan farm at Port Royal (2026-07-13) each returned only the 'DRY by definition until the founder approves + deploys the StructureFactory' notice — no gold cost, no design, no site info — so the builder rung cannot be priced or saved toward; the plan/read half of the build tool is blocked on the factory deploy, not just the execute half.
- **2026-07-14** [trader] quote returns ok with no payload: quote at Port Royal exits 0 with 'ok' but zero price data on two consecutive ticks (SALT:20→COPPER, COD:11→COPPER, 2026-07-13 and 2026-07-14) — goods cannot be priced for sale, blocking the entire goods-trader rung for a hold-rich/coin-poor player.
- **2026-07-14** [brawler] guard wages unbankable — no clock-out path: pawn 52 has run guard 4d+ (currentRun 4d0h45m) with accumulated stuck at 0d0h0m; two LIVE work re-clocks (2026-07-14) were no-ops (currentRun never reset), and the work tool offers clock-in only. Accrued guard time can never bank into a wage, so the guard job pays nothing regardless of run length.
- **2026-07-14** [fisher] loot payout keeper off 72h+: now 5 consecutive server-verified bilge-rats WINs (7/11 13:27 → 7/14 13:26) paid 0 copper against a flat 96-copper baseline — the from-zero fight rail verifies but banks nothing, so every zero-capital climber is stalled at rung 0 receivables.
- **2026-07-16** [brawler] quote tool returns opaque result: four read-only quotes across two ticks (29 RATIONS→GOLD, 29 RATIONS→COPPER, 19 APPLE→GOLD/COPPER at Port Royal) all returned bare 'ok' with no price, amountOut, or route data — the quote rail is unreadable, so no sell route can ever be confirmed live before executing.
- **2026-07-16** [fisher] haul wage hours never mature: pawn employed in 'haul' with currentRun 1d 23h 35m yet accumulated stays 0d 0h 0m — nearly two full days of clocked work have banked zero wage time, so the honest-labor rail verifies employment but accrues nothing.
- **2026-07-16** [trader] water-pawn read omits promised data: READ mode (pawn 53, target flow) exits ok but returns only the execute-gating note — no level, no flow, no cost — so the documented 'No execute = READ level + cost' contract is unmet and a flow-gated wage can never be diagnosed without spending USDC blind.
- **2026-07-17** [brawler] loot keeper still off after verified win: bilge-rats fight on 2026-07-16 returned WIN (server-verified) but wallet showed copper 98 before, same-tick after, and again on 2026-07-17 — a verified win banked zero copper and zero goods, so the fight rail's payout keeper remains dead despite the rail being the designed from-zero income path.
- **2026-07-17** [fisher] no goods-sell rail: SALT 48 / COD 11 quote fine against GOLD but no tool executes the sale — trade is buy-only (gapId) and fish sell requires FISH — so a hold full of loot goods cannot be converted into the seed GOLD the fisher loop needs; rung 1 (goods trader) is unreachable from the menu.
- **2026-07-18** [brawler] fish loop circular from zero: at Port Royal with gold=0 the fish rail is unenterable — 'sell' demands >=10 FISH (held COD:11 is not accepted despite being fish-category), 'catch' demands GOLD via the ocean GOLD->FISH swap, so a zero-gold player holding fish goods can neither sell nor catch; the advertised from-zero cash-out leg dead-ends without prior GOLD.
- **2026-07-18** [citizen] unattributed GOLD credit: wallet gold rose 31898→36662 (+4764, ~$47) between 07-17 and 07-18 with claimable=0, no claims fired by me, and the cbBTC convert batch confirmed UNFIRED ($7.68 < $10 threshold) — no rail in the action menu accounts for the credit; balances should be attributable to a named rail
- **2026-07-18** [worker] water-pawn flow READ returns no data: with correct args (pawn:0, target:flow) the read exits ok but prints only the READ-ONLY/live-requirements boilerplate — no current flow level and no $ cost — so a player cannot check whether zero job-flow is why 12d of haul accrual has paid nothing.
- **2026-07-19** [worker] claim-achievement can't see non-Guard pawns: the READ scans 70 pawns but only citizen-owned GUARD pawns on the GOLD line (0 matched), and there is no line/pawn-type arg — so my Redrum Raiders' day-1 rogue-line rung is unreadable and unclaimable from the tool, hiding the charter's richest early move.
- **2026-07-19** [fisher] loot payout keeper off 8+ days: 9 consecutive server-verified fight WINs since 7/11 have paid 0 copper against a flat 96-copper baseline — the only from-zero income rail verifies wins but banks nothing, so rung 0 cannot accumulate
- **2026-07-19** [trader] loot payout keeper still off (2nd confirmation): a second server-verified bilge-rat WIN (2026-07-18, pawn 0x2E2AB7ae48876f1b4497A04d864C025f7DF58e1f:53 at Port Royal) again banked zero — copper frozen at 95 in the 2026-07-19 snapshot, matching the 7/16 probe; wins verify but never pay out
- **2026-07-20** [citizen] lumber gap not executable: the ONLY actionable Port Report gap ('lumber') has failed two consecutive $0.25 trade attempts with 'route not executable (custom pool / no fill)' — the report flags it actionable but the route has no fill, so the one advertised trade opportunity is a dead end; either the actionable flag should require executable liquidity or the lumber pool needs seeding.
- **2026-07-20** [worker] quote returns bare string: COPPER→GOLD quote of 97 copper at Port Royal exited ok but printed only the literal string 'quote' — no rate, no output amount, no floor info — on both allowed attempts (2026-07-18, 2026-07-19), leaving small-stack coin-ladder liquidity unreadable in-game.
- **2026-07-20** [trader] scan-gaps flags a non-executable gap as actionable: Port Report lists lumber as the top gap with actionable:true, but the trade DRY planner refuses it (exit 1, custom pool / no fill) at Port Royal — the actionable flag misleads bots into planning routes that cannot fill
- **2026-07-22** [fisher] goods→gold no execution rail: I can quote SALT/COD→GOLD (read-only 'ok') but no action-menu tool executes a food-good→GOLD swap, so a hold full of goods cannot be turned into the seed gold a first catch requires — held goods are stranded wealth.
- **2026-07-23** [worker] haul payout unreachable after 17 days: my pawn 0x4ECe491951B759363bCBAF75389a202Fe0584080:0 has currentRun 16d 23h 58m at job=haul with accumulated frozen at 10d 22h 15m since 2026-07-11; no in-game lever converts currentRun→copper (work clock-in returns bare 'work', mode:2 semantics undocumented and untestable without risking accrual reset), so the entire honest-labor experiment is stalled with no reachable wage payout at Port Royal.
- **2026-07-23** [trader] zero-capital player has NO executable income rail: at Port Royal a 1-pawn/0-gold/0-flow player faces every from-zero path down at once — loot keeper OFF (verified wins bank 0, copper frozen 95), all pricing dead (quote empty, build DRY, trade lumber no-fill), no attested rungs, and the only employed job (barter) shows 16d23h run with exactly 0 accrued. This contradicts the charter premise that the climb is reachable from nothing; a new player literally cannot earn a first copper.
- **2026-07-23** [worker] builder rung un-priced 10+ days: build {kind:mill} and {kind:farm} at Port Royal still return no gold price as of 2026-07-23, so the savings target that decides whether 97 copper + any future wage is weeks or months from a first structure remains unknowable — StructureFactory pricing has not shipped.
- **2026-07-23** [worker] copper stack illiquid vs trade floor: 97 copper ≈ $0.0097 sits ~10x under minUsdPerTrade $0.10, so even a valid COPPER→GOLD quote cannot execute — small-stack coin conversion is floor-gated until ~1000 copper, stranding the only liquid coin I have.
- **2026-07-24** [brawler] quote results not journaled: six quote steps on 2026-07-21 and 07-22 (SALT->COPPER 49, COPPER->GOLD 98, COD->GOLD 11) each recorded only the string 'quote' as the outcome — no amounts, no prices — so the read-only pricing rail produces no usable data for planning.
