{
  "name": "mft-launch",
  "version": "1.0.0",
  "description": "Launch an unruggable token for $1",
  "scripts": {
    "compile": "npx hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
